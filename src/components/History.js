const History = () => {
    return (
        <div>
            Work History Component -- will load history state from GET request
            Will also render HistoryForm after onClick to add more hx
        </div>
    )
};

export default History;